# frozen_string_literal: true

source 'https://rubygems.org/'

gem 'debug', platforms: %i[mri mingw x64_mingw] # To debug ruby code
gem 'rubocop', require: false # Ruby static code analyzer and code formatter
